class Knife{
	 
	 String type;
	 double length;
	 
	 Knife(String type, double length){
		 this.type = type;
		 this.length = length;
	 }
	 
	 void display(){
		System.out.println("type : " + this.type);
		System.out.println("length : " + this.length);
	}
 }
 
